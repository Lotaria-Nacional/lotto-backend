version: '3.9'

services:
  postgres:
    image: postgres:17
    container_name: postgres
    restart: always
    environment:
      POSTGRES_USER: lotaria_nacional
      POSTGRES_PASSWORD: Lp6YtX4SNq10
      POSTGRES_DB: intern_managment_db_prod # banco padrão produção
    ports:
      - '5432:5432'
    volumes:
      - intern_managment_db_prod_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: redis
    restart: always
    ports:
      - '6379:6379'
    volumes:
      - redis_data:/data

volumes:
  intern_managment_db_prod_data:
  redis_data: